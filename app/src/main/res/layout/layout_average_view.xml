<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools">

    <ProgressBar
        android:id="@+id/average_view_progress_bar"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:layout_gravity="center"
        android:background="@drawable/circle_shape"
        android:progress="70"
        android:progressDrawable="@drawable/circle_progress_bar"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/average_view_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="3.00"
        app:layout_constraintBottom_toBottomOf="@+id/average_view_progress_bar"
        app:layout_constraintEnd_toEndOf="@+id/average_view_progress_bar"
        app:layout_constraintStart_toStartOf="@+id/average_view_progress_bar"
        app:layout_constraintTop_toTopOf="@+id/average_view_progress_bar" />


</androidx.constraintlayout.widget.ConstraintLayout>